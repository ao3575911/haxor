# Hello, Haxor! — the traditional first program
let name = "Haxor"
let version = "0.1.0"

print(f"Hello from {name} {version}!")
print("A minimal language for 2030.")

# Basic arithmetic
let x = 6 * 7
print(f"6 × 7 = {x}")

# String interpolation
let items = ["formal verification", "plugins", "REPL"]
for item in items:
    print(f"  ✓ {item}")
